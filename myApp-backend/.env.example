PORT=3001
NODE_ENV=development

# CORS Configuration (для production)
# Список дозволених доменів через кому
# ALLOWED_ORIGINS=https://your-app.com,https://www.your-app.com

# Cache Configuration (опціонально)
# CACHE_TTL=60000
# CACHE_MAX_SIZE=100
